'use strict';

let fs = require('fs');
let path = require('path');

class LogHelper {
    /**
     * Writes a line to the current log
     *
     * @param {String} line
     */
    static writeLine(line) {
        
    }
}

module.exports = LogHelper;
